package examples.java.awt.image;

import java.awt.image.BufferedImage;
import java.util.Random;

/**
 * <p>Example of using {@link CropImageFilter}
 * class to crop a {@link BufferedImage}. This example creates a random image and applies the filter to crop it.</p>
 * 
 * <p>This example is useful for demonstrating how to use filters with complex behaviors. Filters are commonly used in image processing, so understanding and using filters effectively can be quite valuable.</p>
 */
public class CropImageFilter_setPixels_Example {

    // ... (truncated for brevity)

    /**
     * <p>Applies the crop filter to a given image. The resulting image will have its contents cropped according to the specified rectangle coordinates.</p>
     * 
     * @param sourceImage  the original image that needs to be filtered
     * @param x  the horizontal coordinate of the top-left corner of the rectangle area to crop from the source image
     * @param y  the vertical coordinate of the top-left corner of the rectangle area to crop from the source image
     * @param width  the width of the rectangle area to crop from the source image
     * @param height  the height of the rectangle area to crop from the source image
     * @return the resulting cropped image after applying the filter
     */
    public static BufferedImage applyCropFilter(BufferedImage sourceImage, int x, int y, int width, int height)) {

        // ... (truncated for brevity)

    }

}