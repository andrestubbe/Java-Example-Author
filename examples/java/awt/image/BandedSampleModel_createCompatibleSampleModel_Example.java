package examples.java.awt.image;

import java.awt.image.BandedSampleModel;

/**
 * Example of creating a BandedSampleModel object and using its methods to manipulate the sample model data.
 */
public class BandedSampleModel_createCompatibleSampleModel_Example {

    /**
     * Creates a new BandedSampleModel object with specified parameters.
     * @param numBands The number of bands in the image, must be at least 1 and less than or equal to 255.
     * @param width The horizontal pixel offset within the image for the top-left corner of the rectangle to be drawn.
     * @param height The vertical pixel offset within the image for the top-left corner of the rectangle to be drawn.
     * @return A new BandedSampleModel object with specified parameters.
     */
    public static BandedSampleModel createCompatibleSampleModel(int numBands, int width, int height)) {
        // Code implementation here
    }
}